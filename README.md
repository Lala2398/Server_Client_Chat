# Server_Client_Chat
A small multi-client chat application written in C for Linux, using BSD sockets (TCP). A central server relays messages between any number of clients (up to `MAX_CLIENTS`), so several people can talk in one room.
